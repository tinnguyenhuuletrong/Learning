[package]
edition = "2021"
exclude = ["index.node"]
name = "rs_node_bind"
version = "0.1.0"

[lib]
crate-type = ["cdylib"]

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]

rs_play = {path = "../../app/rs-play"}

[dependencies.neon]
default-features = false
features = ["napi-6"]
version = "0.10"
