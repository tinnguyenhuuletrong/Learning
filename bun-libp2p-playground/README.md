# bun-libp2p

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run index.ts

# Bun got error to run 
# Failed to bind socket

# Use node 22
npx tsx src/local-discovery.ts
```

https://github.com/libp2p/js-libp2p-examples/blob/main/examples/js-libp2p-example-discovery-mechanisms/README.md

<!-- This project was created using `bun init` in bun v1.1.31. [Bun](https://bun.sh) is a fast all-in-one JavaScript runtime. -->
